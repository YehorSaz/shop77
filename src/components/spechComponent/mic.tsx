import React from 'react';
import { StyleSheet, View } from 'react-native';

export const Mic = () => {
  return <View style={styles.wrapper}>Mic</View>;
};

const styles = StyleSheet.create({
  wrapper: {},
});
